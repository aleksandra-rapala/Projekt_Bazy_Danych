import redis

def connect_redis():
# # Połączenie z bazą danych Redis
# r = redis.Redis(host='localhost', port=6379, db=0)
   
def disconnect_mongo(client_mongo):
# # Zamknięcie połączenia z bazą danych Redis
# r.close()

def mongo_select(col_mongo):
# # Utworzenie wpisu
# r.hmset('nazwa_tabeli', {'ID': 1, 'NAME': 'John'})

# # Wykonanie polecenia SELECT
# results_redis = r.hgetall('nazwa_tabeli')
# for key, value in results_redis.items():
#     print(key.decode(), value.decode())

# def mongo_delete(cur_pg):

# def mongo_update(cur_pg):

# def mongo_insert(cur_pg):
